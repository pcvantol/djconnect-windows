# DJConnect Windows 3.2.10

- Adds DJ announcement output choices for this device, Home Assistant speaker, both or text-only.
- Adds local HTTP and WebSocket Home Assistant contract fixture checks for CI without real backend services.
- Keeps pairing compatible with current Home Assistant builds by sending all supported pairing-code field names.
