# Windows Roadmap Index

This is local navigation only; central planning remains in `pcvantol/djconnect`.

The governance adoption and native-preflight consumer adoption are merged. The
Windows smoke service-diagnostics increment is also merged and reconciled. The
3.3.0 ARM64 deployment completed successfully, but its service-session smoke
could not qualify a GUI application. The next scoped increment is a
least-privilege interactive-session smoke relay. It must reuse existing
deployment evidence and must not redeploy the unchanged artifact.
