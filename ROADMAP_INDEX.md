# Windows Roadmap Index

This is local navigation only; central planning remains in `pcvantol/djconnect`.

The governance adoption, native-preflight consumer adoption and smoke
diagnostics are merged and reconciled. The 3.3.0 ARM64 deployment completed
successfully, but service-session smoke could not qualify a GUI application.
The current reviewable increment installs and consumes a least-privilege
interactive-session smoke relay. It reuses existing deployment evidence and
must not redeploy the unchanged artifact.
