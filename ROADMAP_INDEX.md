# Windows Roadmap Index

This is local navigation only; central planning remains in `pcvantol/djconnect`.

The governance adoption is merged. Current work is the Windows native-preflight consumer-adoption increment. After its merge, rerun the already authorized manifest-bound ARM64 deployment and dispatch target-scoped smoke only after deployment success.
