# Windows Roadmap Index

This is local navigation only; central planning remains in `pcvantol/djconnect`.

The governance adoption and native-preflight consumer adoption are merged.
The 3.3.0 ARM64 deployment completed successfully. Current work is the Windows
smoke service-diagnostics increment. After its merge, rerun target-scoped smoke
against the existing successful deployment evidence; do not redeploy first.
