name: Post-Merge Release Evidence

on:
  workflow_run:
    workflows: [CI]
    types: [completed]

permissions:
  actions: read
  checks: read
  contents: read
  pull-requests: read
  statuses: write

jobs:
  reconcile:
    if: github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main'
    uses: pcvantol/djconnect/.github/workflows/post-merge-release-evidence.yml@6ff2be61d8cd4041ac4bf21ef622ef330a0bc14a
    with:
      policy_source_ref: 2c65089aa2654e749852ab82728119e8c106c1ad
      required_main_workflow: CI
      repository_role: active_source
      coverage_artifact_name: windows-cobertura-coverage
