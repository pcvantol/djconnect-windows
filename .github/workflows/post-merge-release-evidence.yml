name: Post-Merge Release Evidence

on:
  workflow_run:
    workflows: [CI]
    types: [completed]

permissions:
  actions: read
  checks: read
  contents: write
  pull-requests: read
  statuses: write

jobs:
  reconcile:
    if: github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main'
    uses: pcvantol/djconnect/.github/workflows/post-merge-release-evidence.yml@4931f1371b53159d837968955a7b4972051bdcbe
    with:
      policy_source_ref: 4931f1371b53159d837968955a7b4972051bdcbe
      required_main_workflow: CI
      required_check_contexts: '["Trusted Delivery qualification / Qualify trusted delivery"]'
      repository_role: active_source
      coverage_artifact_name: windows-cobertura-coverage
